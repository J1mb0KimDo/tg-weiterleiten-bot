services:
  - type: web
    name: telegram-bot
    env: python
    plan: free   # oder starter (empfohlen!)
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: true